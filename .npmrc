# Dandelion install configuration
fund=false
audit=false

# Native modules (better-sqlite3) are rebuilt against Electron's ABI explicitly
# via `npm run rebuild` rather than during install, so a missing toolchain does
# not abort the whole install. See docs/DEVELOPER.md.
